@extends('layouts.app') @section('content')<div class="card"><h2>Dashboard Super Admin</h2><p><a class="btn" href="{{ route('superadmin.fasilitas.index') }}">Master Fasilitas Kost</a></p></div><div class="card"><h3>Validasi Admin Kost</h3><table><tr><th>Nama</th><th>Kost</th><th>Status</th><th>Aksi</th></tr>@foreach($admins as $u)<tr><td>{{ $u->nama }}<br>{{ $u->username }}</td><td>{{ $u->kost?->nama_kost }}</td><td>{{ $u->status }}</td><td>@if($u->status==='pending')<form method="post" action="{{ route('superadmin.admin.validasi',$u) }}" style="display:inline">@csrf<button class="ok">Validasi</button></form><form method="post" action="{{ route('superadmin.admin.tolak',$u) }}" style="display:inline">@csrf<button class="danger">Tolak</button></form>@endif</td></tr>@endforeach</table></div>@endsection
