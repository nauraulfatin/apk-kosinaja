@extends('layouts.app') @section('content')<div class="card"><h2>Fasilitas Kamar: {{ $kamar->nama_kamar }}</h2><form method="post" action="{{ route('admin.kamar.fasilitas.update',$kamar) }}">@csrf @method('put')@foreach($fasilitas as $f)<label><input type="checkbox" name="fasilitas[]" value="{{ $f->id_fasilitas }}" @checked(in_array($f->id_fasilitas,$selected)) style="width:auto"> {{ $f->nama_fasilitas }}</label><br>@endforeach<br><button>Simpan</button></form></div>@endsection
